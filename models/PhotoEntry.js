class PhotoEntry {
  constructor(id, title, image, comment) {
    this.id = id.toString();
    this.title = title;
    this.image = image;
    this.comment = comment;
  }
}

export default PhotoEntry;